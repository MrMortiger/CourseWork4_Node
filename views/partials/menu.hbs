<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style/css/Home.css">
</head>
<body>
    <script>
        function sortTable(n) { //Сортування
                var table, rows, switching, i, x, y, shouldSwitch, dir, switchcount = 0;
                table = document.getElementById("table1");
                switching = true;
                dir = "asc";
                while (switching) {
                    switching = false;
                    rows = table.getElementsByTagName("TR");
                    for (i = 1; i < (rows.length - 1); i++) {
                        shouldSwitch = false;
                        x = rows[i].getElementsByTagName("TD")[n];
                        y = rows[i + 1].getElementsByTagName("TD")[n];
                        if (dir == "asc") {
                            if (x.innerHTML.toLowerCase() > y.innerHTML.toLowerCase()) {
                                shouldSwitch = true;
                                break;
                            }
                        } else if (dir == "desc") {
                            if (x.innerHTML.toLowerCase() < y.innerHTML.toLowerCase()) {
                                shouldSwitch = true;
                                break;
                            }
                        }
                    }
                    if (shouldSwitch) {
                        rows[i].parentNode.insertBefore(rows[i + 1], rows[i]);
                        switching = true;
                        switchcount++;
                    } else {
                        if (switchcount == 0 && dir == "asc") {
                            dir = "desc";
                            switching = true;
                        }
                    }
                }
            }
    </script>
    <div class="home">
        <div class="home-page"><!--Home-->
            <div class="home-slogan"><!--Home-img and slogan-->
                <img class="home-img" src="style/design/site-img/blue-clouds-day-fluffy-53594.jpeg" alt="Sky-img">
                <h1 class="slogan">Вільні, як птахи у небі</h1>
            </div>
            <div class="control-panel"><!--Control-menu-->
                <button class="dB-menu-item" onclick="document.location='/'">
                    <p class="dB-menu-item-text">Home</p>
                </button>
                <button class="dB-menu-item" onclick="document.location='/personTable'">
                    <p class="dB-menu-item-text">Person</p>
                </button>
                <button class="dB-menu-item" onclick="document.location='/flightTable'">
                    <p class="dB-menu-item-text">Flight</p>
                </button>
            </div>
        </div>
    </div>
</body>
</html>